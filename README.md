# makedep

`makedep` generates Makefile.dep (a makefile) encapsulating the dependencies of Fortran programs.
It simply scans a list of source paths for all source code and does a rudimentary analysis of the module dependencies.

`makedep` was originally conceived and written to work within the Modular Ocean Model, version 6, (MOM6) project.

